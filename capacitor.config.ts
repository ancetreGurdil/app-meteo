import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.meteo.app',
  appName: 'app-meteo',
  webDir: 'dist/app-meteo',
  bundledWebRuntime: false
};

export default config;
